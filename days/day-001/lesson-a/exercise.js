// Day 001 — Lesson A (Foundations)
// Type: Foundations
// Tech: js
//
// Today you will implement a few small, pure functions that operate on arrays.
// For each function, you should also think about its time complexity.
//
// INSTRUCTIONS:
// 1. Read each function description carefully.
// 2. Implement the function body.
// 3. In a comment ABOVE each function, write your best guess at the time
//    complexity in terms of Big-O (e.g. O(1), O(n)).
// 4. Do NOT use any external libraries. Stick to plain JavaScript.
// 5. Aim for clear naming and readable code.

/**
 * sumArray(numbers)
 * -----------------
 * Given an array of numbers, return the sum of all elements.
 *
 * Requirements:
 * - Do not mutate the input array.
 * - Handle the empty array by returning 0.
 *
 * Example:
 *   sumArray([1, 2, 3]) === 6
 */
function sumArray(numbers) {
  // TODO: implement
}

/**
 * countOccurrences(items, value)
 * ------------------------------
 * Given an array `items` and a `value`, return how many times `value` appears.
 *
 * Requirements:
 * - Use a simple loop (no fancy methods required).
 *
 * Example:
 *   countOccurrences(['a', 'b', 'a'], 'a') === 2
 */
function countOccurrences(items, value) {
  // TODO: implement
}

/**
 * minAndMax(numbers)
 * ------------------
 * Given an array of numbers, return an object with the smallest and largest
 * values: { min: number, max: number }.
 *
 * Requirements:
 * - If the array is empty, return null.
 * - Iterate the array at most once.
 *
 * Example:
 *   minAndMax([3, 1, 10]) === { min: 1, max: 10 }
 */
function minAndMax(numbers) {
  // TODO: implement
}

// OPTIONAL: add a few manual tests at the bottom of the file (e.g. console.log)
// to check that your functions behave as expected.
