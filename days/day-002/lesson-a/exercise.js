// Day 002 — Lesson A (Foundations)
// Type: Foundations
// Tech: js
//
// Today you will practice working with strings and conditionals.
// Focus on writing clear, small functions and thinking about edge cases.
//
// INSTRUCTIONS:
// 1. Implement each function described below.
// 2. Above each function, add a comment with your Big-O time complexity guess.
// 3. Keep your code safe for unexpected inputs where noted in the description.

/**
 * isLongerThan(text, maxLength)
 * -----------------------------
 * Return true if `text` has more than `maxLength` characters, false otherwise.
 *
 * Requirements:
 * - If `text` is null or undefined, treat it as an empty string.
 */
function isLongerThan(text, maxLength) {
  // TODO: implement
}

/**
 * areEqualIgnoreCase(a, b)
 * ------------------------
 * Return true if strings `a` and `b` are equal when compared case-insensitively.
 *
 * Requirements:
 * - If either value is null or undefined, return false.
 * - Use `.toLowerCase()` for comparison.
 */
function areEqualIgnoreCase(a, b) {
  // TODO: implement
}

/**
 * countDigits(text)
 * -----------------
 * Return how many characters in `text` are digits 0–9.
 *
 * Requirements:
 * - Treat non-string inputs as an empty string.
 * - You may find `text[i] >= '0' && text[i] <= '9'` useful.
 */
function countDigits(text) {
  // TODO: implement
}

// OPTIONAL: add some console.log calls to check your work, for example:
// console.log(isLongerThan("hello", 3)); // true
// console.log(areEqualIgnoreCase("Hello", "hELLo")); // true
// console.log(countDigits("Room 101B")); // 3
