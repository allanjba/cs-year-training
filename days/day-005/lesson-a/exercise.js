// Day 005 — Lesson A (Foundations)
// Tech: js (JavaScript)
//
// Today you will practice "map" and "filter" style transformations using loops.
// For each function you write:
// - Add a brief Big-O time complexity comment.
// - Note at least one non-trivial edge case.

/**
 * doubleNumbers(numbers)
 * ----------------------
 * Return a NEW array where each number is doubled.
 *
 * Example:
 *   doubleNumbers([1, 2, 3]) => [2, 4, 6]
 */
function doubleNumbers(numbers) {
  // TODO: implement
}

/**
 * filterLongWords(words, minLength)
 * ---------------------------------
 * Return a NEW array containing only words whose length is >= minLength.
 *
 * Requirements:
 * - Do not modify the original `words` array.
 */
function filterLongWords(words, minLength) {
  // TODO: implement
}

/**
 * extractFirstNames(users)
 * ------------------------
 * `users` is an array of objects with at least a `firstName` field.
 * Return an array containing only the first names.
 *
 * Example:
 *   extractFirstNames([{ firstName: "Ada" }, { firstName: "Alan" }])
 *   => ["Ada", "Alan"]
 */
function extractFirstNames(users) {
  // TODO: implement
}

/**
 * filterActiveUsers(users)
 * ------------------------
 * `users` is an array of objects with an `isActive` boolean field.
 * Return a NEW array containing only users where `isActive` is true.
 */
function filterActiveUsers(users) {
  // TODO: implement
}

// OPTIONAL: manual checks
// console.log(doubleNumbers([1, 2, 3]));
// console.log(filterLongWords(["a", "hello", "world"], 3));
// console.log(extractFirstNames([{ firstName: "Ada" }, { firstName: "Alan" }]));
